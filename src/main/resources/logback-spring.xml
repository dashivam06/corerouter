<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty name="APP_NAME"
                    source="spring.application.name"
                    defaultValue="corerouter"/>

    <include resource="org/springframework/boot/logging/logback/defaults.xml" />

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %5p [${APP_NAME},%X{requestId:-},%X{userId:-},%X{worker:-}] --- [%15.15t] %-40.40c{1} : %m%n
            </pattern>
        </encoder>
    </appender>

    <!-- Azure SDK cache/netty mismatch warnings are noisy and non-fatal in this setup. -->
    <logger name="com.azure.core.http.netty.implementation.Utility" level="ERROR"/>
    <logger name="com.microsoft.aad.msal4j.ConfidentialClientApplication" level="ERROR"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
